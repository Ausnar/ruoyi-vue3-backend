# 外出拜访 dev 验收归档 SQL

本目录仅存放外出拜访模块在 `ruoyi_ai_dev` 上使用过的 mock / 验收 / 回放脚本。

约定如下：

- 这些脚本不属于当前正式基线
- 不应作为 `ruoyi/sql/dev` 的常规结构变更脚本执行
- 不应复制到 `ruoyi/sql/prod`
- 仅在需要复测、演示、回放历史验收场景时按需使用

当前已归档：

- `20260414_visit_passive_event_v2_mock_acceptance_dev.sql`
- `20260416_visit_passive_contract_multi_external_mock_acceptance_dev.sql`
